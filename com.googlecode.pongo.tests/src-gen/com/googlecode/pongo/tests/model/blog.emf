package com.googlecode.pongo.tests.model;

@collection
class Post {
   @searchable
   attr String title;
   val Comment[*] comments;
}

class FeaturedPost extends Post {
   attr String reason;
}

class Comment {
   attr String author;
   val Comment[*] replies;
}